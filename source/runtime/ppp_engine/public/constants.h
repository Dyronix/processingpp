/**
 * @file constants.h
 * @brief Mathematical constants.
 */
#pragma once

namespace ppp
{
    /** @brief Return ?/2. */
    float half_pi();
    /** @brief Return ?. */
    float pi();
    /** @brief Return ?/4. */
    float quarter_pi();
    /** @brief Return 2?. */
    float two_pi();
    /** @brief Return ? (tau), equal to 2?. */
    float tau();
}
/**
 * @file constants.h
 * @brief Mathematical constants.
 */
#pragma once

namespace ppp
{
    /** @brief Return pi/2. */
    float half_pi();
    /** @brief Return pi. */
    float pi();
    /** @brief Return pi/4. */
    float quarter_pi();
    /** @brief Return 2pi. */
    float two_pi();
    /** @brief Return tau. */
    float tau();
}
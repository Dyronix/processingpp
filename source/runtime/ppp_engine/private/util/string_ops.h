#pragma once

#include <string>

namespace ppp
{
    namespace string
    {
        std::string string_replace(const std::string& subject, const std::string& search, const std::string& replace);
    }
}